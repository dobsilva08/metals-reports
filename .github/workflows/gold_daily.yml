name: Gold — Relatório Diário (PIAPI + Fallback)

on:
  schedule:
    # 09:00 UTC ≙ 06:00 BRT
    - cron: "0 9 * * *"
  workflow_dispatch:
    inputs:
      preview:
        description: "Enviar para CHAT DE TESTE (se TELEGRAM_CHAT_ID_TEST estiver definido)"
        type: boolean
        default: false

concurrency:
  group: metals-gold-daily
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest

    env:
      # ===== LLM (padrão: PIAPI) =====
      PIAPI_API_KEY: ${{ secrets.PIAPI_API_KEY }}
      PIAPI_MODEL: gpt-4o-mini

      # Fallbacks (opcionais)
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      GROQ_MODEL: llama-3.1-70b-versatile
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      OPENAI_MODEL: gpt-4o-mini
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      DEEPSEEK_MODEL: deepseek-chat

      LLM_PROVIDER: piapi
      LLM_FALLBACK_ORDER: piapi,groq,openai,deepseek

      # ===== Telegram =====
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID_METALS: ${{ secrets.TELEGRAM_CHAT_ID_METALS }}
      TELEGRAM_CHAT_ID_TEST: ${{ secrets.TELEGRAM_CHAT_ID_TEST }}
      TELEGRAM_MESSAGE_THREAD_ID: ${{ secrets.TELEGRAM_MESSAGE_THREAD_ID }}

      # ===== Dados (opcionais) =====
      FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      GOLDAPI_KEY: ${{ secrets.GOLDAPI_KEY }}
      ALPHA_VANTAGE_API_KEY: ${{ secrets.ALPHA_VANTAGE_API_KEY }}
      NASDAQ_DATA_LINK_API_KEY: ${{ secrets.NASDAQ_DATA_LINK_API_KEY }}
      METAL_PRICE_API: ${{ secrets.METAL_PRICE_API }}
      METALS_DEV_API: ${{ secrets.METALS_DEV_API }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # ---------- CACHE DIÁRIO DOS .SENT ----------
      - name: Definir data UTC do cache
        id: date
        run: echo "date=$(date -u +%F)" >> $GITHUB_OUTPUT

      - name: Preparar pasta de sentinelas
        run: mkdir -p data/sentinels

      - name: Cache sentinelas (por dia)
        uses: actions/cache@v4
        with:
          path: data/sentinels
          key: sentinels-${{ github.workflow }}-${{ steps.date.outputs.date }}
          restore-keys: |
            sentinels-${{ github.workflow }}-

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: (Opcional) Verificar PiAPI
        if: ${{ env.PIAPI_API_KEY != '' }}
        continue-on-error: true
        run: |
          python scripts/tools/check_piapi.py

      # PREVIEW manual (usa TELEGRAM_CHAT_ID_TEST, se definido)
      - name: Rodar Gold Diário (PREVIEW)
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.preview == true }}
        run: |
          python ./scripts/gold_daily.py --send-telegram --preview

      # Execução normal (agendado OU manual preview=false)
      - name: Rodar Gold Diário
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.preview != true }}
        run: |
          python ./scripts/gold_daily.py --send-telegram
